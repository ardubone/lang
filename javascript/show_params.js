let engine = '249 hp'; 
let turbo = true ;
let maxSpeed = 260 ;
// назначения переменных turbo и maxSpeed

console.log(engine, turbo, maxSpeed); // вывод переменных engine, turbo и maxSpeed
console.log(typeof engine, typeof turbo, typeof maxSpeed); // вывод типа переменных engine, turbo и maxSpeed