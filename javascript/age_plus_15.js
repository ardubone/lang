function plus15(currentAge)  // установка функции
{
  let age = 51;
  return age + 15 ; // возврат с данными
}
let newAge = plus15(age) ; // вызов функции с параметром age
alert ('А через 15 лет будет ' + newAge ); // вывод текста + данные